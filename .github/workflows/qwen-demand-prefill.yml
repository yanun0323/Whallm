name: Qwen demand prefill

on:
  push:
    branches: [feat/qwen-flash-optimize]
    paths: ['.github/qwen-demand-candidate/**', '.github/workflows/qwen-demand-prefill.yml']
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: qwen-demand-prefill-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    runs-on: macos-15
    timeout-minutes: 30
    env:
      DEVELOPER_DIR: /Applications/Xcode_26.3.app/Contents/Developer
      PYTHONPATH: runtime:.
      MLX_ENABLE_TF32: '0'
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
      - name: Apply reviewed candidate
        run: python .github/qwen-demand-candidate/apply.py
      - name: Install pinned runtime
        id: install
        run: python -m pip install -r requirements.txt
      - name: Validate native expert reads, Qwen state and benchmark contracts
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p validation
          make test-qwen-flash-portable PYTHON=python 2>&1 | tee validation/portable.log
          python -u - <<'PY' 2>&1 | tee validation/python.log
          import json, os, platform, unittest
          from pathlib import Path
          from importlib.metadata import version
          import mlx.core as mx
          from deepseek_v4_ssd.throughput_diagnostics import source_files_hash
          if not mx.metal.is_available():
              raise RuntimeError('Metal unavailable; no CPU substitution.')
          mx.set_default_device(mx.gpu)
          metadata = dict(commit=os.environ['GITHUB_SHA'], candidate_applied=True,
              source_files_sha256=source_files_hash(), platform=platform.platform(),
              mlx=version('mlx'), device=mx.device_info(), full_model_benchmark=False)
          loader = unittest.defaultTestLoader
          patterns = ['test_qwen*.py', 'test_throughput.py', 'test_prefill*.py', 'test_expert*.py', 'test_ssd_prefill*.py']
          suite = unittest.TestSuite(loader.discover('runtime/tests', pattern=p) for p in patterns)
          result = unittest.TextTestRunner(verbosity=2).run(suite)
          metadata.update(tests_run=result.testsRun, errors=len(result.errors), failures=len(result.failures),
              skipped=[dict(test=str(t), reason=r) for t,r in result.skipped], success=result.wasSuccessful())
          Path('validation/python-summary.json').write_text(json.dumps(metadata, indent=2) + '\n')
          raise SystemExit(not result.wasSuccessful())
          PY
      - name: Compare selected and reused native expert bytes at equal budgets
        shell: bash
        run: |
          set -euo pipefail
          python -u Scripts/benchmark_qwen_demand_prefill.py --output validation/demand-prefill.json 2>&1 | tee validation/demand-prefill.log
      - name: Validate full App suite, localized controls and runtime catalogs
        if: ${{ !cancelled() && steps.install.outcome == 'success' }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p validation
          export WHALLM_QWEN_UI_ARTIFACTS="$GITHUB_WORKSPACE/validation"
          { swift --version; sw_vers; git rev-parse HEAD; } | tee validation/environment.log
          plutil -lint Sources/DeepSeekV4SSDApp/Resources/*.lproj/Localizable.strings
          make test 2>&1 | tee validation/swift.log
          python Scripts/validate_qwen_flash_app_catalog.py validation/catalogs 2>&1 | tee validation/catalog.log
          python -m compileall -q runtime Scripts
          git diff --check
      - name: Preserve evidence including failures
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: qwen-demand-validation
          path: validation/
          retention-days: 14
          if-no-files-found: error

  publish:
    needs: validate
    if: github.event_name == 'push' && github.ref == 'refs/heads/feat/qwen-flash-optimize'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Publish validated source without rewriting history
        shell: bash
        env:
          EXPECTED_HEAD: ${{ github.sha }}
        run: |
          set -euo pipefail
          python3 .github/qwen-demand-candidate/apply.py
          test "$(git ls-remote origin refs/heads/feat/qwen-flash-optimize | cut -f1)" = "$EXPECTED_HEAD"
          git rm -r .github/qwen-demand-candidate
          git add Makefile Scripts Sources Tests runtime docs
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'perf(qwen): add exact route-first prefill and resident expert reuse'
          git push origin HEAD:refs/heads/feat/qwen-flash-optimize
